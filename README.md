# How to use it
Add following snippets to the pom.xml file:

```xml
<repositories>
    <repository>
        <id>robb-mvn-repo</id>
        <url>https://rawgit.com/hxh-robb/mvn-repo/master</url>
    </repository>
</repositories>
```
